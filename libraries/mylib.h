// library of useful functions

#include <vector>

using namespace std;

// function to calculate the standard deviation of the mean
double var (double, double, int);

vector<double> MeanBlock (vector<double>, int, int);

// function to randomly generate an angle between 0 and 2*pi with using pi
double Theta (double , double );

double min (double , double );